name: DAG - MATERNAL_MORTALITY_DATA (PROD)
on:
  workflow_dispatch:
jobs:
  call-staging-workflow:
    uses: SatcherInstitute/health-equity-tracker/.github/workflows/dagMaternalMortality.yml@main
    secrets: inherit
